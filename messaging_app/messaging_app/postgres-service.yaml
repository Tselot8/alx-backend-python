apiVersion: v1
kind: Service
metadata:
  name: db  # 👈 This must match the HOST in Django settings
spec:
  selector:
    app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432