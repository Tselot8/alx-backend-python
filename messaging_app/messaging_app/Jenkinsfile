pipeline {
    agent any

    environment {
        GITHUB_CREDENTIALS = credentials('github-alx-backend')
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Tselot8/alx-backend-python.git',
                    credentialsId: 'github-alx-backend'
            }
        }

        stage('Setup Python') {
            steps {
                bat 'python -m venv venv'
                bat 'call venv\\Scripts\\activate && pip3 install --upgrade pip'
                bat 'call venv\\Scripts\\activate && pip3 install -r messaging_app/requirements.txt'
            }
        }

        stage('Run Tests') {
            steps {
                bat 'call venv\\Scripts\\activate && pytest messaging_app\\tests --junitxml=report.xml'
            }
        }

        stage('Archive Report') {
            steps {
                junit 'report.xml'
            }
        }
    }
}
